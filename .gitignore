# Build artifacts
build/
*.o
*.a
*.so
*.dylib

# Editors / OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Third-party paper materials (arxiv 2605.23923) -- excluded from the public
# repo to respect the authors' copyright.  Download yourself with:
#   curl -L -o source.tar.gz https://arxiv.org/e-print/2605.23923
#   tar -xzf source.tar.gz
paper.tex
bib-Delgado-Lp-2016.bib
FT_SU2.png
source.tar.gz

# perf data
perf.data*

# Python
__pycache__/
*.pyc
.venv/

# Weather-demo cache (NOAA NCEP/NCAR reanalysis NetCDF; ~3-10 MB, re-fetchable)
data/*.nc
data/*.nc.cache

# Julia (SU2FFT.jl bindings — bead su2fft-t6z)
# Manifest is environment-specific; deps.jl and build.log are generated by Pkg.build.
julia/Manifest.toml
julia/deps/deps.jl
julia/deps/build.log

# af proof local state that isn't worth versioning
proof/locks/

# Generated spectrum images live under build/ already (covered above)

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
